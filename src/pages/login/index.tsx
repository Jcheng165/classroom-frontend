/**
 * Login page.
 *
 * Thin wrapper around the shared Refine sign-in form component.
 */
import { SignInForm } from "@/components/refine-ui/form/sign-in-form";

export const Login = () => {
  return <SignInForm />;
};
