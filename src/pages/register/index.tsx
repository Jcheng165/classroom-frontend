/**
 * Register page.
 *
 * Thin wrapper around the shared Refine sign-up form component.
 */
import { SignUpForm } from '@/components/refine-ui/form/sign-up-form';

export const Register = () => {
  return <SignUpForm />;
};
